name: Main
on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Generate Changelog
        run: echo "# Good things have arrived" > ${{ github.workspace }}-CHANGELOG.txt
      - name: Release
        uses: softprops/action-gh-release@v2
        with:
            name: 'aaabbb fasd fads'
            tag_name: 'fortest-fsa-ff'
            token: ${{ secrets.GH_TOKEN }}
            files: ${{ github.workspace }}/configs/x86_64.config
            body: |
                **This is OpenWrt Firmware for **fasdf
                ### 📒 固件信息
                - 💻 平台架构: 'x86_64'
                - ⚽ 固件源码: 'openwrt'
